server:
  port: 8084

spring:
  application:
    name: task-service

logging:
  config: classpath:bestwo-logback-spring.xml
  level:
    root: INFO
    com.bestwo.dataplatform.task: INFO

bestwo:
  logging:
    slow-request-threshold-ms: ${BESTWO_LOG_SLOW_REQUEST_THRESHOLD_MS:1000}

server:
  port: 8085

spring:
  application:
    name: sync-service

logging:
  config: classpath:bestwo-logback-spring.xml
  level:
    root: INFO
    com.bestwo.dataplatform.sync: INFO

bestwo:
  logging:
    slow-request-threshold-ms: ${BESTWO_LOG_SLOW_REQUEST_THRESHOLD_MS:1000}

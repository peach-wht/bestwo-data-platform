server:
  port: 8080

spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      routes:
        - id: auth-service-placeholder
          uri: http://auth-service:8081
          predicates:
            - Path=/auth/**
